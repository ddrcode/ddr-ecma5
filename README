ddr-ecma5 is a tiny library (3,8kB minified) which brings the ECMAScript 5 extensions 
to any browser and older JavaScript interpreters (it works properly outside browser 
environment). It includes set of ECMAScript 5 methods to Object, Array, String, Date 
and Function objects which are possible to be implemented with ECMAScript 3 features.

The goal of ddr-ecma5 library is to promote ECMAScript 5 features and to bring them 
to the environments of older browsers. 

The library is stable and properly unit tested with QUnit.

